import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => ({
  form: {
    height: "100%",
    maxHeight: "100%",
  },
  stack: {
    height: "100%",
    maxHeight: "100%",
  },
  editor: {
    height: `85%`,
    maxHeight: "85%",
    overflow: "auto",
  },
}));

export default useStyles;
